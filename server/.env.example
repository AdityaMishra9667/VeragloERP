# Copy to server/.env
DATABASE_URL=postgresql://veraglo:veraglo@localhost:5432/veraglo_erp
PORT=3000
CORS_ORIGIN=http://localhost:3000

# Optional — Open-Meteo is used by default (no key required)
# OPENWEATHER_API_KEY=
